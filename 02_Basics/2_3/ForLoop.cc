#include <cstdint> //OK
#include <iostream>

int main()
{
    // FOR-Schleife (Iteration)
    // Anzahl Durchläufe ist bereits bekannt

    for (int i = 0; i < 5; i++)
    {
        std::cout << i << std::endl;
    }

    return 0;
}
