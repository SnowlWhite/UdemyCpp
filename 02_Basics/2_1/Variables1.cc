#include <iostream> //OK

// 1.) Can not start with a number
// 2.) Can not contain whitespaces
// 3.) Variable name must be unique

int main(){

    int my_integer_value; // Deklaration
    my_integer_value = 2; // Initialisierung (Wertezuweisung)

    float my_float_value = 3.5; // Deklaration + Initialisierung

    return 0;
}
